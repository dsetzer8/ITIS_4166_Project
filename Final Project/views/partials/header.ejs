<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, intial-scale=1.0" />
        <title>Index</title>
        <link rel="stylesheet" href="/css/main.css" />
    </head>
    <!--Header-->
    <div class = "header">
    <!--Logo - Logo is also linked to the Home Page /Index Page-->
        <a href = "/" class = "logo"><img src="/images/lofty.png" alt="Lofty"></a>
    
    <!--Nav Bar: Users-->
    <div class = "nav">
        <%if(!user){%>
            <a href='/users/new'>Sign Up</a>
            <a href='/users/login'>Login</a>
        <%} else {%>
            <a href='/users/profile'>Profile</a>
            <a href='/users/logout'>Logout</a>
        <%}%>
    </div> 

    
    <!--Representative links:-->
        <div class = "header-right">
    <!--Start a new connection-->
    <!--"Start a new connection" is linked to the newConnection.html-->
            <a href = "/connections/newConnections">Start A New Connection</a>     
        </div>    
    </div>
    <!--Connections-->
    <div class="title">
        <a href="/connections">Connections</a>
    </div>
    <hr> 

    <% if(errorMessages.length > 0) {%>
        <% errorMessages.forEach(message=>{%>
            <div class='error'>
                <%=message%>
            </div>
        <%});%>
    <%}%>
    
    <% if(successMessages.length > 0) {%>
        <% successMessages.forEach(message=>{%>
            <div class='success'>
                <%=message%>
            </div>
        <%});%>
    <%}%>